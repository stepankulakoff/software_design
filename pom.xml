<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>
    <groupId>ru.homework</groupId>
    <artifactId>currency-homework</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <!-- Сначала клиент публикует контракт, потом сервер проверяет его. -->
    <modules>
        <module>rate-printer</module>
        <module>rate-provider</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <curator.version>5.9.0</curator.version>
        <pact.version>4.6.17</pact.version>
        <pact.broker.url>http://localhost:9292</pact.broker.url>
        <pact.branch>hw3</pact.branch>
        <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
        <pact.app.version>${maven.build.timestamp}</pact.app.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.apache.curator</groupId>
                <artifactId>curator-recipes</artifactId>
                <version>${curator.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <systemPropertyVariables>
                            <pact_do_not_track>true</pact_do_not_track>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
